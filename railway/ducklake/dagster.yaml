storage:
  postgres:
    postgres_db:
      username:
        env: PG_USER
      password:
        env: PG_PASSWORD
      hostname:
        env: PG_HOST
      db_name:
        env: PG_DATABASE
      port:
        env: PG_PORT
